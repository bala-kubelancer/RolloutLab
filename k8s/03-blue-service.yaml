apiVersion: v1
kind: Service
metadata:
  name: syklab-blue
  namespace: syklab
spec:
  selector:
    app: syklab
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
