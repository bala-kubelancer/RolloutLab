apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: syklab-gateway
  namespace: syklab
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - syklab.dev.kubelancer.net
